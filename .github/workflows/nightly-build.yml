name: Package Nightly Build

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
    package-and-release:
      uses: ./.github/workflows/package-and-release.yml
      secrets: inherit
      with:
        tag: nightly
        is-prerelease: 'true'
        is-rolling-tag: true
        generate-release-notes: false
